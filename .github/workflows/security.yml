name: Security Scan

on:
  push: # Trigger on pushes to any branch
  pull_request:
    branches: 
      - main

permissions:
  contents: read
  pull-requests: read

jobs:
  scan:
    uses: 5103-Runtime/.github/.github/workflows/gitleaks.yml@main
    secrets: inherit